use bevy::prelude::*;
use forky_play::spline::ecs_graph::*;
use forky_play::*;
use sweet::*;

sweet! {
	it "works" {




		// expect(true).to_be(false)?;

		let graph = EcsSplineGraph::new(
			EcsSplineGraphId(0),
			Handle::default()
		);
		let mut commands = Commands::default();


	}
}
