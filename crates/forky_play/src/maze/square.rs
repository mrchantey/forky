// use super::{_rect_maze, MazeGraph};

// pub fn new(width: usize) -> MazeGraph { _rect_maze::new(width, width) }
// pub fn draw_maze(graph: &MazeGraph, width: usize) -> Vec<u8> { _rect_maze::draw_maze(graph, width, width) }
// pub fn draw(width: usize) -> Vec<u8> { _rect_maze::draw(width, width) }
// pub fn format(grid: &Vec<u8>, width: usize) -> String { _rect_maze::format(grid, width, width) }
