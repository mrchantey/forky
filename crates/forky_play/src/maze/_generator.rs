pub struct MazeGraph {
	head: Node,
	tail: Node,
}

pub struct Node {
	// pub neighbors: &[Node],
}

impl Node {}


pub fn init_square(width: usize) {
	// let head = Node { neighbors: &[] };
	let tail = Node {};
	// head.neighbors = [&tail];
	// for x in 0..len {
	// 	for y in 0..len {}
	// }
	// let a = skdlds;

	// head
}
