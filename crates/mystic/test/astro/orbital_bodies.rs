use mystic::astro::planets::*;
use sweet::*;

sweet! {
	it "works" {


		let system = SolarSystem::new(Y2000Day(0.));
		// println!("{:?}", system.bodies[&Body::Sun].ecliptic);
		// let earth = OrbitalBody::new(
		// expect(true).to_be(false)?;

	}
}
