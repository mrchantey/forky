use mystic::astro::planets::*;
use sweet::*;

sweet! {
	test skip "equatorial" {

		// let coords = EquatorialCoords{
		// 	right_ascention:3.,
		// 	declination:8.,
		// 	radius:1.,
		// };
		// let rect = coords.to_rectangular();
		// let coords2 = rect.to_equatorial();
		// expect(coords2).to_be(coords)?;

	}

	test skip "ecliptical" {

		// let coords = EclipticalCoords::new{
		// 	longitude:3.,
		// 	latitude:8.,
		// 	radius:1.,
		// };
		// let rect = coords.to_rectangular();
		// let coords2 = rect.to_ecliptical();
		// expect(coords2).to_be(coords)?;
	}
}
